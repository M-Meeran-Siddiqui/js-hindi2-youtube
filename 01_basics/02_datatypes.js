"use strict"; //treat all JS code as newer version

// alert(3 + 3);   //we are using nodejs , not browser

console.log(3 + 3); // code readability should be high.

console.log("Meeran");

let name = "hitesh";
let age = 18
let isLoggedIn  = false
let state = null; //standalone value;



//number => 2 to power 53
// bigint
// String => ""
// boolean => true/false
// null => standalone value   it means  that it is the representation of empty value. And also is a special type.
// undefined =>     it means we declare a variable but  not assign any value.
// symbol => use for find the uniqueness.   


// Object

console.log(typeof 'a');
console.log(typeof age);

console.log(typeof null);        // object (is the typeof null).
console.log(typeof undefined);  // iska output undefined he ata hai kyuki  ye khud mein ek type hai.
